package polymorphsms;

// import java.util.Scanner;

public class Main2 {
    public static void main(String[] args) {
        // Polymorphism = many shapes/forms
        //                ability of a method to identify object of different classes as having of the same kind
        //                one method with multiple implementations
        //                allows methods to do different things based on the object it is acting upon
        //                allows you to define one interface and have multiple implementations
        //                allows you to have many classes that are related to each other by inheritance

        // Car car = new Car();
        // Bicycle bicycle = new Bicycle();
        // Boat boat = new Boat();

        // Vehicle[] racers = {car, bicycle, boat}; // polymorphism

        // // car.go();

        // for (Vehicle x : racers) { // polymorphism
        //     x.go();
        // }

        // Dynamic Polymorphism = method overriding
        //                        occurs during runtime
        //                        a call to an overridden method is resolved at runtime
        //                        cannot override static methods
        //                        can override methods that are inherited
        //                        can only override non-static methods
        //                        can only override methods with the same name, return type, and parameters

        // Scanner scanner = new Scanner(System.in);
        // Animal animal;

        // System.out.println("What animal do you want?");
        // System.out.println("1. Dog");
        // System.out.println("2. Cat");

        // int choice = scanner.nextInt();

        // if (choice == 1) {
        //     animal = new Dog();
        //     animal.speak();
        // } else if (choice == 2) {
        //     animal = new Cat();
        //     animal.speak();
        // } else {
        //     animal = new Animal();
        //     System.out.println("Invalid choice");
        //     animal.speak();
        // }

        
    }  
}
