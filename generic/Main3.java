package generic;

public class Main3 {
    public static void main(String[] args) {
        // bounded types = types that specify a bound for the generic type
        // bounded types are used to restrict the types that can be used as type arguments in a parameterized type
        // a type argument can be a subtype of the bounded type
        // you can declare a bounded type parameter by using the extends keyword followed by the type
        // you can use the extends keyword to specify that a type parameter must be a subtype of a certain class
        // you can use the extends keyword to specify that a type parameter must be a subtype of a certain interface

        
    }
}
