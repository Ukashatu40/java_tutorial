package keybindings;

public class Main {
    public static void main(String[] args) {
        // Key Bindings = used to make a key and mouse click do the same thing
        // (or any other action)
        // Key Bindings are more customizable and flexible
        // Key Bindings are more difficult to implement than KeyListeners
        // Key Bindings are an alternative to KeyListeners
        // Key Bindings are used to bind a key to an Action
        // Key Bindings are used to bind a key to a component
        // Key Bindings are used to bind a key to a key stroke


        Game game = new Game();
    }
}
